spring:
  datasource:
    url: jdbc:mysql://localhost:3306/apii?serverTimezone=Asia/Shanghai
    username: root
    password: mysql123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8088